table:
  schema: core
  name: topics
object_relationships:
  - name: agreement
    using:
      foreign_key_constraint_on: agreement_id
insert_permissions:
  - role: authenticated
    permission:
      check:
        agreement:
          group:
            group_users:
              user:
                user_id:
                  _eq: X-Hasura-User-Id
      columns:
        - agreement_id
        - created_at
        - id
        - index
        - name
        - updated_at
select_permissions:
  - role: authenticated
    permission:
      columns:
        - agreement_id
        - created_at
        - id
        - index
        - name
        - updated_at
      filter:
        agreement:
          group:
            group_users:
              user:
                user_id:
                  _eq: X-Hasura-User-Id
update_permissions:
  - role: authenticated
    permission:
      columns:
        - index
        - name
        - updated_at
      filter:
        agreement:
          group:
            group_users:
              user:
                user_id:
                  _eq: X-Hasura-User-Id
      check: null
delete_permissions:
  - role: authenticated
    permission:
      backend_only: false
      filter:
        agreement:
          group:
            group_users:
              user:
                user_id:
                  _eq: X-Hasura-User-Id
