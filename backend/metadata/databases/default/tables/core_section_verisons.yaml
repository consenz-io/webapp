table:
  name: section_verisons
  schema: core
object_relationships:
  - name: section
    using:
      manual_configuration:
        column_mapping:
          section_id: id
        insertion_order: null
        remote_table:
          name: sections
          schema: core
