table:
  schema: core
  name: users
array_relationships:
  - name: user_groups
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: core
          name: users_groups
select_permissions:
  - role: authenticated
    permission:
      columns:
        - id
        - email
        - provider
        - user_id
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      allow_aggregations: true
