table:
  schema: core
  name: users
array_relationships:
  - name: user_groups
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: core
          name: users_groups
