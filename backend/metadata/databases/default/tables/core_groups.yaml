table:
  schema: core
  name: groups
array_relationships:
  - name: group_users
    using:
      foreign_key_constraint_on:
        column: group_id
        table:
          schema: core
          name: users_groups
  - name: preliminary_agreements
    using:
      foreign_key_constraint_on:
        column: group
        table:
          schema: core
          name: preliminary_agreements
